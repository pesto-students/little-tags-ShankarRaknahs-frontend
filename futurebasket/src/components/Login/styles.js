import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  btn: {
    marginTop: '10%',
    marginBottom: '10%',
  },
  phone: {
    marginTop: '5%',
    marginBottom: '10%',
  },
  row: {
    display: 'flex',
    flexDirection: 'row',
  },
});
